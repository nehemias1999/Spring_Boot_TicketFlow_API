server:
  port: 8084

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: ticketflow-secret-key-must-be-at-least-32-chars-long!!
  expiration-ms: 86400000  # 24 hours
